#ifndef LIST_H
#define LIST_H
#include<stdio.h>
#include<string.h>
#include<limits.h>
#define MAX_COUNT (sizeof(unsigned long int) * 8)

int _putchar(char c);
unsigned int binary_to_uint(const char *b);

#endif
